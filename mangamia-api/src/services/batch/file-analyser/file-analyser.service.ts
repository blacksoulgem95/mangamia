import { Injectable } from '@nestjs/common';

@Injectable()
/**
 * Scans the file system for new, undiscovered files, and runs plugins if any allowed for the file
 */
export class FileAnalyserService {}
